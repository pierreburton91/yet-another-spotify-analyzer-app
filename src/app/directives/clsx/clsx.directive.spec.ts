import { ClsxDirective } from './clsx.directive';

describe('ClsxDirective', () => {
  it('should create an instance', () => {
    const directive = new ClsxDirective();
    expect(directive).toBeTruthy();
  });
});
